{% extends 'base.html.twig' %}

{% block body %}

{% include "_header.html.twig" %}

<section class="main container-fluid container-lg">
    <h1 class="text-center my-4">Comment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ comment.text }}</td>
            </tr>
            <tr>
                <th>Timedate</th>
                <td>{{ comment.timedate }}</td>
            </tr>
        </tbody>
    </table>

    {#<a href="{{ path('comment_index') }}">back to list</a>

    <a href="{{ path('comment_edit', {'id': comment.id}) }}">edit</a>#}

    {{ include('comment/_delete_form.html.twig') }}
</section>

{% include "_footer.html.twig" %}

{% endblock %}
