<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav w-100">
            <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
            <div class="d-flex flex-column flex-lg-row ml-lg-auto">
            {% if is_granted("ROLE_USER") %}
                <li class="nav-item"><span class="nav-link no-hover">Hello {{app.user.username}}</span></li>
                {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item"><a class="nav-link" href="/videogames/new">Create product</a></li>
                    
                {% endif %}
                <li class="nav-item"><a class="nav-link" href="/logout">Log out</a></li>
            {% else %}
                <li class="nav-item"><a class="nav-link" href="/login">Log in</a></li>
                <li class="nav-item"><a class="nav-link" href="/register">Register</a></li>
            {% endif %}
            </div>
        </ul>
    </div>
</nav>